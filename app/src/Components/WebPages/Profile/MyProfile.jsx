import React from 'react';
import {Card, CardText, CardTitle} from "reactstrap";

const MyProfile = ({reload, setReload}) => {


    return (
        <Card>
            <CardTitle>Coming Soon...</CardTitle>
        </Card>
    )
}
export default MyProfile;